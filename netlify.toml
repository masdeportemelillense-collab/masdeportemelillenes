[build]
  command = "node scripts/with-app-env.mjs vite build && node scripts/netlify-publish.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = ".netlify/functions-internal"
